[pytest]
# auto — async-тесты без декоратора @pytest.mark.asyncio
asyncio_mode = auto
# Фикстуры живут в session-scoped петле asyncio — одна БД/engine на сессию
asyncio_default_fixture_loop_scope = session
testpaths = tests
